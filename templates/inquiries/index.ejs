<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="../css/reset.css" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="../bootstrap/dist/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="../bootstrap/dist/css/bootstrap-theme.min.css">
    <link rel="stylesheet" href="../css/style.css">
    <title>Document</title>
</head>

<body>
    <h1>Заявки</h1>
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>
            <div id="myNavbar" class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-left">
                    <li><a href="/seo">Клиенты</a></li>
                    <li><a href="">Запросы</a></li>
                    <li><a href="">Оплаты</a></li>
                </ul>
                <% if (user.length) { %>
                    <ul class="nav navbar-nav navbar-right">
                        <li style="background-color: #6466AB;"><a href="" style="color: #fff">Новых заявок: (<%= user.length %>)</a></li>
                    </ul>
                <% } %>
            </div>
        </div>
    </nav>
    <% if (user.length) { %>
        <div class="container">
            <div class="row">
                <input type="text" placeholder="Быстрый поиск">
                <table class="table">
                    <thead>
                        <tr>
                            <th>№</th>
                            <th>Компания</th>
                            <th>Имя</th>
                            <th>Телефон</th>
                            <th>Сайт</th>
                            <th>E-mail</th>
                            <th>Действия</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for (var i = 0; i < name.length; i++) { %>
                            <tr>
                                <td class="id"><%= i + 1 %></td>
                                <td class="text"><%= user[i].name %></td>
                                <td class="text"><%= user[i].company %></td>
                                <td class="text"><%= user[i].phone %></td>
                                <td class="text"><%= user[i].urlSite %></td>
                                <td class="text"><%= user[i].email %></td>
                                <td><a class="btn" href="inquiries/<%= user[i].id %>">Подтвердить</a></td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    <% } else { %>
        <div class="container">
            <div class="row">
                <h1>Заявок нет</h1>
            </div>
        </div>
    <% } %>

    <script src="jquery/dist/jquery.min.js" type="text/javascript"></script>
    <script src="bootstrap/dist/js/bootstrap.min.js" type="text/javascript"></script>
    <script src="js/grid.js" type="text/javascript"></script>
</body>

</html>